主  题：日报
汇报人：陈文通
日  期：2018年7月23日 星期一
内  容：
今日学习了的内容：
   1.复习了数据库常用的语句操作，练习多表级联，为项目建表做准备
   2.了解了dba主要需要学习的知识点，以及初步了解了一下数据库安全设置
   3.跟上老师课程进度，学习了映射及动态SQL
   4.部署Linux开发环境等各种开发软件的重新配备
没有解决的问题：
1）计划中的集合学习未完成
2）未进行算法练习
明天的学习计划：
1）学习集合中的List源码及其特点
2）进一步了解学习数据库安全设置
3）初步学习Linux基本指令操作
4）练习课堂内容
4.学习任务完成度： （60%）
5.对自己今天学习的满意度：（60）分 [注：1-10分]
6.对今天的学习做一句话的总结：真是走了不少弯路，浪费了很多时间，哈哈
附录：
算法1：
题目：在一个二维数组中（每个一维数组的长度相同），每一行都按照从左到右递增的顺序排序，每一列都按照从上到下递增的顺序排序。请完成一个函数，输入这样的一个二维数组和一个整数，判断数组中是否含有该整数
代码：
    public class Solution {
    public boolean Find(int target, int [][] array) {
        int j=array[0].length-1;
        int i=0;
        while(j>=0&&i<array.length) {
        	if(target<array[i][j])
        		j--;
        	else {
        		if(target==array[i][j]) {
        			return true;
        		}
        		else {
        			i++;
        		}
        	}
        }
		return false;
    }
}

算法2：
题目：请实现一个函数，将一个字符串中的每个空格替换成“%20”。例如，当字符串为We Are Happy.则经过替换之后的字符串为We%20Are%20Happy。
代码：
    public class Solution {
    public String replaceSpace(StringBuffer str) {
    	String s=str.toString();
    	String[] s1=s.split("");
    	String s3="";
    	for(int i=0;i<s.length();i++) {
    		char c=s.charAt(i);
    		if(c==' ') {
    			s1[i]="%20";
    		}
    	    s3=s3+s1[i];
    	}
		return s3;
    }
}
【学习内容真实可靠，给自己的评分和完成度客观真实，不谦虚，不夸大】